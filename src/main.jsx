import React from "react";
import ReactDOM from "react-dom/client";
import AnchoredVerse from "./AnchoredVerse.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AnchoredVerse />
  </React.StrictMode>
);
